name: Daily Auto Commit

on:
  schedule:
    - cron: '0 0 * * *' # Runs every day at midnight UTC
  workflow_dispatch: # Allows manual triggering

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'

    - name: Run update script
      run: node index.js

    - name: Commit and push changes
      run: |
        git config --global user.name "om051105"
        git config --global user.email "99085886+om051105@users.noreply.github.com"
        git add README.md
        git commit -m "Auto-commit: Daily streak update" || echo "No changes to commit"
        git push
